export const roomIds = Array.from({ length: 20 }, (_, i) => `room-${i}`);
export const userIds = Array.from({ length: 20 }, (_, i) => `user-${i}`);
export const documentIds = Array.from(
  { length: 20 },
  (_, i) => `document-${i}`,
);
export const spectatorIds = Array.from(
  { length: 20 },
  (_, i) => `spectator-${i}`,
);
