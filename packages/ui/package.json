{
  "name": "@code-sync/ui",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "eslint": "eslint --cache --cache-location ./.eslintcache --cache-strategy content . --ext ts,tsx",
    "lint": "pnpm type-check && pnpm eslint",
    "lint:fix": "pnpm lint --fix",
    "stylelint": "stylelint --cache --cache-location ./.stylelintcache --cache-strategy content **/*.css",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@code-sync/stylelint-config": "workspace:*",
    "@code-sync/eslint-config": "workspace:*",
    "@code-sync/typescript-config": "workspace:*",
    "eslint": "8.57.0",
    "stylelint": "16.3.1",
    "typescript": "5.3.2"
  }
}
